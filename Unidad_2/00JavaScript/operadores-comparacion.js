/**
 * == si un valor es igual a otro.
 * === va a comparar si es del mismo tipo numerico y si tienen el mismo valor
 * !=
 * !== si son diferentes en tipo y en valor
 *  
 * > mayor que
 * < menor que
 * AND -- &&
 * OR --||
 * NOT |
 * 
 * 
*/
const a = 10;
const b = 20;
const c = "30";

console.log(a == b);
console.log(a ===b);
console.log(a == c);